<%= form_with(model: draw_stage, local: true) do |form| %>
  <% if draw_stage.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(draw_stage.errors.count, "error") %> prohibited this draw_stage from being saved:</h2>

      <ul>
        <% draw_stage.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :draw_stage_definition_id %>
    <%= form.text_field :draw_stage_definition_id %>
  </div>

  <div class="field">
    <%= form.label :draw_step_id %>
    <%= form.text_field :draw_step_id %>
  </div>

  <div class="field">
    <%= form.label :group_id %>
    <%= form.text_field :group_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
